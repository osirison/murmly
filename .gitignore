__pycache__/
.pytest_cache/
*.pyc
*.egg-info/
.venv/
build/
dist/

# Git worktrees checked out inside the repository. A worktree is another
# branch, not content of this one, and `git add -A` otherwise commits it as a
# stray submodule reference.
.worktrees/

# The assembled Pages artifact: site/ copied over the generated manual. Built in
# CI from committed sources, never committed itself.
_pages/
